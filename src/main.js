import { createApp } from 'vue'
import App from './App.vue'

// If you decide to manage Tailwind CSS via npm and PostCSS with Vite,
// you would import its main CSS file here, e.g.:
// import './index.css' 

const app = createApp(App)
app.mount('#app')
